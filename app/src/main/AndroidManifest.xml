<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jmapps.simplenotes">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:alwaysRetainTaskState="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddMainNoteActivity"
            android:alwaysRetainTaskState="true"
            android:label="@string/add_note"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".ModifyMainNoteActivity"
            android:alwaysRetainTaskState="true"
            android:label="@string/modify_note"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:name=".CategoryContentActivity"
            android:alwaysRetainTaskState="true"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ModifyCategoryNoteActivity"
            android:alwaysRetainTaskState="true"
            android:label="@string/modify_note"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateUnchanged|adjustResize" />
        <activity
            android:name=".AddCategoryNoteActivity"
            android:alwaysRetainTaskState="true"
            android:label="@string/add_note"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateVisible|adjustResize" />
    </application>

</manifest>